# 仕上げ前チェックリスト

## 5分で見る項目
- `python3 -m http.server 8000` で起動できる
- `npm run verify:release` が通る
- `小選挙区` タブで都道府県と選挙区チップが機能する
- `比例代表` タブでブロック切り替えが機能する
- `検索` タブでヒットあり、0件、再読込後の復元が機能する
- カードの表裏切り替えが機能する
- 390px 幅で横スクロールが出ない
- オフライン時に `offline.html` へ退避できる
- ホーム画面追加案内が見える
- ホーム画面追加後に上端の余白や固定タブが不自然に詰まらない
- 一覧スクロール時に写真読み込みで大きく引っかからない
- 氏名と顔写真の対応を数件目視で確認する

## 実機で最後に見る項目
- iPhone Safari で実 URL から開ける
- ホーム画面追加ができる
- ホーム画面起動で違和感が少ない
- 表面が顔写真だけで使える
- タップしづらいボタンがない
- 写真が縦長でも崩れない
